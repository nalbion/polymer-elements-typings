/// <reference path="../neon/NeonAnimationBehavior.d.ts"/>

declare module slide {
	/* 
		`<slide-up-animation>` animates the transform of an element from `translateY(0)` to
		`translateY(-100%)`. The `transformOrigin` defaults to `50% 0`.
		
		Configuration:
		```
		{
		  name: 'slide-up-animation',
		  node: <node>,
		  transformOrigin: <transform-origin>,
		  timing: <animation-timing>
		}
		```
	*/
	export class SlideUpAnimation  extends Polymer.NeonAnimationBehavior
	{
	
	

	
	}
}
