/// <reference path="../neon/NeonAnimationBehavior.d.ts"/>

declare module scale {
	/* 
		`<scale-up-animation>` animates the scale transform of an element from 0 to 1. By default it
		scales in both the x and y axes.
		
		Configuration:
		```
		{
		  name: 'scale-up-animation',
		  node: <node>,
		  axis: 'x' | 'y' | '',
		  transformOrigin: <transform-origin>,
		  timing: <animation-timing>
		}
		```
	*/
	export class ScaleUpAnimation  extends Polymer.NeonAnimationBehavior
	{
	
	

	
	}
}
